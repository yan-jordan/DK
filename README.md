# تسک‌های فنی — هَندآپ

پاسخ به ۳ تمرین مستقل «راهنمای انجام تسک‌های فنی». هر تمرین در پوشه‌ی خودش، مستقل از بقیه.

## تمرین ۱ — موتور محاسبه‌ی هزینه‌ی ارسال «بازار»

[`task1/`](task1/) — موتور محاسبه‌ی هزینه‌ی ارسال بر اساس ۹ قاعده‌ی داده‌شده که در چند نقطه با هم مبهم/متناقض بودند.

- کد: `task1/src/`
- مستند تصمیم‌ها (مهم‌ترین بخش این تمرین): [`task1/DECISIONS.md`](task1/DECISIONS.md)
- اجرا و تست: [`task1/README.md`](task1/README.md)

## تمرین ۲ — طراحی ایجنت خودکار رفع تیکت

[`task2/`](task2/) — سند تحلیلی، بدون کد (طبق فرمت تحویل این تمرین).

## تمرین ۳ — سرویس REST روی دیتاست IMDb

[`task3/`](task3/) — سرویس FastAPI روی دیتاست کامل IMDb، با دو اندپوینت، ساخت ایندکس با DuckDB، و سرو با `mmap`.

- اجرا با یک دستور: `docker compose up --build` (داخل `task3/`)
- جزئیات، تصمیم‌ها، اعداد اندازه‌گیری‌شده: [`task3/README.md`](task3/README.md)
